Questlog TL Farm Planner v21.9 UPDATE

SELF-UPDATER FOUNDATION

This is intended to be the last update that normally requires manual ZIP installation.

New:
- public GitHub Releases update channel
- Settings -> Updates
- top Check for Updates / status badge
- optional startup update check
- release-note display
- recognized stable or versioned planner ZIP assets
- required GitHub SHA-256 asset digest
- independent local SHA-256 verification
- 100 MB update download limit
- ZIP path-safety validation
- external update helper applies files only after the server exits
- changed root/payload files backed up before live replacement
- automatic server restart
- existing browser-tab reconnect flow reused
- live update result included in System Health + Diagnostic Bundle
- rollback now removes files introduced by a live update

IMPORTANT ONE-TIME FOLLOW-UP:
After installing v21.9, configure a public GitHub Releases repository in:
Settings -> Updates

Use OWNER/REPOSITORY.

No scan, cache clear, setup, or planner-state migration required.

Manual ZIP updating remains available as fallback.
